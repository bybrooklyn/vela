# Upstream patches

Keep generated patch files here when a version-pinned Signal-iOS, libsignal, or
RingRTC adaptation cannot live behind `VelaSignalBridge` or `PlatformKit`.
Every patch must have a matching entry in
`docs/porting/UPSTREAM_PATCHES.md`, an owner, and an interoperability test.
