# Upstream Patch Ledger

No upstream-owned files are present in this overlay archive, so there are no
persistent upstream patches yet.

Use this format once installed into a direct Signal-iOS fork:

```markdown
## PATCH-0001 — <title>

- Upstream files:
- Mac requirement:
- Minimal change:
- iOS behavior impact:
- Tests:
- First applied at upstream commit:
- Tracking issue:
```

Prefer new adapters and files. Set a patch budget and review it during every
upstream merge.
